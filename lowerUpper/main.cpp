#include <iostream>
#include <vector>
#include <string>
using namespace std;

bool isUpper(string s)
{
    bool result = false;

    for (size_t i = 0; i < count; i++)
    {
        /* code */
    }
    

    return result;
}

int main()
{
    vector<string> lower, upper;

    string.s;
    while (cin >> s)
    {
        if (isUpper(s))
        {
            upper.push_back(s);
        }
        else
        {
            lower.push_back(s);
        }
        
    }



    return 0;
}